## v0.2.0 — 界面重做 + 自己选游戏 + 首次启动设备检测

发布日期：2026-09-20

这一版解决三件使用体验上的硬伤：**界面挤成一团**、**只能靠自动识别猜游戏**、
**麦克风坏了也不知道为什么没反应**。

### ✨ 新增

**1. 启动前先做设备检测（麦克风 + 扬声器）**

第一次打开软件，主界面之前会先弹一个四步向导：

- **麦克风**：对着话筒说话，屏幕上有一条实时电平条。看到它跳起来就说明
  麦克风没问题 —— 不用懂什么采样率、设备索引，能看见条动就行。
- **扬声器**：分别测 左声道 / 双声道 / 右声道 三个测试音，用来确认耳机左右
  没接反（测试音是现场合成的，不带音频文件）。
- 选好的输入 / 输出设备自动写进设置，之后不用再配。
- 每一步都能**跳过**，向导绝不拦着你用软件。
- 已经走过向导的用户升级后不会被打扰；想重测可以在
  **高级设置 → 声音设备 → 重新检测**。

**2. 首页：自己选「当前在玩的游戏」**

以前只能等软件靠进程扫描去猜，猜不到就一直是待机。现在首页有一张卡片：

- 点「选择当前游玩的游戏」→ 弹出列表，里面**只列出此刻真的有窗口开着的程序**
  （所以 driver、后台服务、更新器这类噪音不会出现），游戏还会排在前面。
- 每个程序后面标了「已有配置『xxx』」还是「新建配置」，一眼看清。
- 点中你正在玩的游戏 → 自动建配置（或复用已有的）→ **直接跳到语音按键编辑页**，
  不用自己再去别的页面找一遍。
- 支持搜索、重新扫描、显示系统组件，以及**手动输入进程名**（窗口标题为空的游戏）。

**3. 界面全面重做（这才是「完全混乱」的真凶）**

之前所有页面挤在一个不可滚动的容器里，窗口一小就互相压叠；同时主题里那条
`QWidget { background-color }` 通配样式把卡片里的每个标签都涂成了实心色块，
圆角卡片被切碎 —— 两者叠加就成了「一团乱」。

现在：

- 新增统一页面骨架 `ScrollPage`：标题固定、内容可滚动，窗口再小也只是出
  滚动条，**不会**重叠。
- 主题里那条通配背景选择器被删掉，改成按 `objectName` 精确指定；文字控件
  统一透明背景，卡片圆角终于完整了。
- 首页 / 编辑页 / 配置页 / 高级设置统一间距、卡片、分组框样式，不再各写一套数字。
- 侧栏底部固定显示当前版本号和配置目录，状态栏也带上版本号与数据位置。

### 🐛 修复

- **编辑页输入一个字符就丢光标**：以前每改一个值都会触发仓库回调重建表格，
  正在输入的单元格被换掉。现在落盘时用标志位挂起刷新，编辑体验正常了。
- **配置页改一个值，整个表单被重新填一遍**（连带滑块跳回原位）：同样用挂起
  刷新的方式解决。
- **「捕获按键」原本是让你手打键名**，现在改成真正的按键捕获控件，
  按什么键就录什么键。
- **百分比 / 数字类输入**改用原生数值控件（带 `%` 后缀），框内只放数字，
  不再靠手打文本，避免填出非法值。
- 配置页右侧详情区改成可滚动，分组再也不用挤在一起。
- **同一个游戏被反复新建重复配置**：进程名归一化用了 `rstrip(".exe")`，
  而 `rstrip` 是按字符集裁剪的 —— `"gvkprobe".rstrip(".exe")` 会得到
  `"gvkprob"`（结尾的字母被啃掉），存取两侧算出不同的名字就再也匹配不上。
  现在改为严格判断后缀的 `normalize_process_name()`，全项目统一调用。

### ✅ 自检

`python -m scripts.selftest` —— **31 项断言全部通过**（v0.1.1 为 25 项）。

新增 6 项覆盖本次改动：

- 运行中程序枚举可用且字段完整（进程名 / 展示名 / 窗口标题）
- 进程名归一化不会误伤结尾字母（修掉 `rstrip(".exe")` 把 `gvkprobe` 啃成
  `gvkprob`、导致同一进程反复新建重复配置的坑）
- 游戏识别启发式（常见游戏目录 → 判定为游戏）
- `Engine.adopt_process()`：用户指定进程 → 建配置 / 复用配置，且不受自动切换开关影响
- 首次启动向导的「已走过」状态写入后可读回（升级不会被重弹）
- 输出设备枚举与测试音接口在无硬件时也能安全返回，不抛异常

### 📋 安装与升级

- 单文件 exe，Windows 10/11 64-bit，无需额外依赖。
- **从 v0.1.1 升级**：直接用新 exe 覆盖旧的即可。首次启动会检测到版本变化，
  自动把你的旧配置备份到 `backups\<时间戳>-upgrade-0.1.1-to-0.2.0\`。
- 因为向导状态存在设置里，老用户升级后**不会**被要求重新做设备检测。
- 想回到旧配置：高级设置 → 数据与备份 → 选那份快照 → 恢复选中快照。

---

## v0.1.1 — 数据安全加固：多用户隔离 + 升级不丢设置

发布日期：2026-09-20

这一版不发新功能，专门解决两件**会让用户真的损失数据**的事：

1. 同一台电脑上不同 Windows 用户之间，配置不能互相看到、不能互相覆盖
2. 每次软件更新（下载新 exe 覆盖旧的），用户辛苦调好的语音指令一条都不能丢

### ✨ 新增

**多用户数据隔离**

- 用户数据固定在 `%APPDATA%\GameVoiceKey\`。Windows 每个登录账户有独立
  `%APPDATA%`，因此不同用户天然各存各的，规则 / 热键 / 麦克风设置永不串味。
- 新增启动体检 `ensure_safe_data_dir()`：如果数据目录被配到了程序目录里
  （这会导致覆盖 exe 时连带删掉数据），自动改回默认位置并给出告警。
- 支持 `GVKEY_DATA_DIR` 覆盖数据位置；**指向程序目录内的路径会被拒绝**并回退。
- UI 与日志里的数据目录一律脱敏显示（`%APPDATA%\GameVoiceKey`），不泄露用户名。

**升级不丢数据（三层保险）**

| 保险 | 触发时机 | 作用 |
|---|---|---|
| 升级自动快照 | 检测到版本号变化 | 升级前把旧数据整份存进 `backups\`，一条不丢 |
| 原子写 + `.bak` | 每次保存配置 | 任何一次写入都留下「上一版良好副本」 |
| 损坏自动恢复 | 读到非法 json | 自动从 `.bak` 恢复，坏文件另存为 `.corrupt-<时间>` |

- 新增「数据与备份」面板（高级设置页顶部）：显示数据目录、隔离状态、
  上次运行版本；支持**立即备份** / **从任意快照恢复** / **删除快照** / 一键打开目录。
- 快照自动保留最近 8 份，超出自动清理，不会无限吃硬盘。
- 恢复操作本身也防呆：覆盖当前数据前会先自动存一份 `pre-restore` 快照。

**向前兼容**

- 新版本加了配置项、之后临时降级到旧版本运行时，旧版本会把不认识的字段
  **原样保留并在写回时带上**，再升级上去设置还在。
- 单个规则里的未知字段同样安全，不会因为多了个字段导致整个 profile 解析失败。

### 🛠️ 工程师细节

- 新增 `gvkey/userdata.py`：数据目录解析 / 隔离校验 / 脱敏显示 / 快照 /
  恢复 / 保留策略 / 版本变化检测 / schema 迁移框架。
- `_atomic_write` 改为「先滚 `.bak` 再落盘」，`_safe_read` 增加从 `.bak`
  自动恢复的分支。
- `Settings` / `Profile` 新增 `extra` 字段承载未知配置项；写入时平铺回顶层，
  保持 json 结构对老版本友好。
- `load_settings()` 检测到 `schema_version` 落后时立即落盘迁移结果，避免重复计算。
- 快照目录名冲突（同秒多次备份）自动加序号，列表顺序稳定。

**打包体积 245MB → 59.5MB**

v0.1.0 的打包脚本用 `--collect-submodules PySide6`，把 WebEngine / Quick /
Multimedia 等上百 MB 用不到的 Qt 模块全收了进来。本应用只用到
QtCore / QtGui / QtWidgets，现已改为显式排除清单，exe 从 245MB 降到 59.5MB，
下载和启动都快了很多（功能一个没少，冒烟测试通过）。

另外打包脚本改为直接调用 PyInstaller、不再做输出重定向：PyInstaller 的进度
写 stderr，一旦被 PowerShell 包成 error record，脚本会误判失败。

### ✅ 自检

`python -m scripts.selftest` —— **25 项断言全部通过**（v0.1.0 为 17 项）。

新增 8 项，专门覆盖本次改动的行为：

- 数据目录不在程序目录内
- `GVKEY_DATA_DIR` 指向程序目录时被拒绝
- 数据目录显示已脱敏（不含 Windows 用户名）
- 快照 → 列表 → 恢复完整闭环
- 快照数量超限自动清理
- **模拟「下载新 exe 覆盖旧 exe」：配置逐项校验不丢，且自动留了升级前快照**
- 未知字段读入 → 写回不丢（Settings 与 Profile 都测）
- `settings.json` 写坏后能自动从 `.bak` 恢复

### 📋 安装与升级

- 单文件 exe，Windows 10/11 64-bit，无需额外依赖。
- **从 v0.1.0 升级**：直接用新 exe 覆盖旧的即可。首次启动会检测到版本变化，
  自动把你的旧配置备份到 `backups\<时间戳>-upgrade-0.1.0-to-0.1.1\`。
- 想回到旧配置：高级设置 → 数据与备份 → 选那份快照 → 恢复选中快照。

---

## v0.1.0 — 首发 Alpha

发布日期：2026-09-20

GameVoiceKey 第一版发布——一款 100% 自定义的全局游戏语音按键映射工具，能自动识别当前游戏，无感切换你的语音指令配置。

### ✨ 新增

**核心功能**

- 智能游戏进程自动识别：后台扫描进程，匹配到你配置的游戏后瞬间无缝切换整套语音指令。
- 多游戏独立存档：每个游戏在 `%APPDATA%\GameVoiceKey\profiles\` 一份 json，规则、参数、开关互不干扰。
- 全自定义语音→按键映射：触发短语 / 按键（单键 / 组合键 / 鼠标 / 滚轮）/ 执行方式（单击 / 长按 / 连发）/ 延迟 / 冷却，全可改。
- 关闭即休眠：游戏关闭瞬间语音监听暂停，桌面 / 浏览器不会误触发。
- 三种语音引擎：能量检测（开箱即用）+ Vosk 离线中文 small 识别（高级页一键下载约 42MB 模型）。

**UI / UX**

- 6 大完整页面：首页（实时波形 + 成功率）、游戏配置管理、语音按键编辑、高级设置、日志与统计、悬浮窗设置。
- 游戏内悬浮窗：3 档尺寸 + 拖拽粘边 + WS_EX_TRANSPARENT 鼠标穿透 + 全屏自动切紧凑模式。
- 系统托盘常驻 + 后台静默 + 关闭时最小化到托盘。
- 深色磨砂玻璃风格（OKLCH 松石绿主题，圆角 12px）。

**安全 / 反作弊**

- 不装游戏插件、不改游戏内存、不读游戏内存，绕过所有反作弊。
- 仅在前台窗口属于当前 Profile 时才生效；你可以随时按全局热键一键暂停。
- 按住说话模式（PTT）可选；启用后才能触发按键。

**自定全局热键**

- 总开关 / 暂停监听 / 重新加载 / 麦克风静音，4 个全局热键全可改（默认 `Ctrl+Alt+V/P/R/M`）。

### 🛠️ 工程师细节

- 原子写 + 损坏文件自动备份（`.corrupt-<时间>`）。
- 应用数据目录与游戏目录 100% 隔离，不会随游戏重装丢失。
- 进程监控白 / 黑名单 + 扫描周期可调。

### 📋 安装与升级

- 单文件 exe，Windows 10/11 64-bit，无需额外依赖。
- 首次启动会自动塞入 `示例 - FPS` / `示例 - MOBA` 两份示例配置。

### 🙏 反馈

Bug 或建议：在 GitHub Issues 提；包含 `%APPDATA%\GameVoiceKey\logs\*.log` 内容与游戏版本更易排查。
